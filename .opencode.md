# 四级真题语境训练助手

## 项目简介
个人使用的四级备考微信小程序，专注听力精听、语境句子学习、翻译自动批改和写作分步教学。所有数据本地存储，AI调用通过本地Node代理服务器访问DeepSeek API。

## 技术栈
- 微信小程序原生开发 + TypeScript
- 本地存储（wx.setStorageSync）
- 本地Node.js代理服务器（Express + Axios）调用DeepSeek API
- 数据文件：miniprogram/data/ 目录下的 .ts 文件

## 核心功能

### 1. 听力精听
- 数据文件：miniprogram/data/listening.ts
- 结构：{ id, title, audioUrl, sentences:[{text,start,end}], fullText }
- 功能：单句循环、显示/隐藏原文、听写填空、变速播放、记录错句

### 2. 句子语境学习
- 数据文件：miniprogram/data/sentences.ts（已提供50句）
- 结构：{ id, english, chinese, keywords, topic }
- 功能：卡片展示、关键词高亮、标记掌握、按话题筛选

### 3. 翻译练习
- 数据文件：miniprogram/data/translations.ts
- 结构：{ id, chinese, reference, source }
- 功能：展示中文 → 用户输入英文 → 调用本地API `/correct_translation` → 显示评分、参考译文、修改建议 → 保存记录

### 4. 写作教学（三级）
- 初级句型库：数据文件 miniprogram/data/sentence_patterns.ts，调用 `/teach_sentence`
- 中级段落写作：给出主题句，用户续写，调用 `/correct_paragraph`
- 高级全文模拟：数据文件 miniprogram/data/writings.ts，调用 `/correct_writing`

### 5. 统计与错题本
- 展示今日概览、错题本、成绩趋势（简单图表）

## 页面设计
- 首页：四个模块入口 + 今日统计
- 听力页：列表 + 精听界面
- 句子页：卡片学习
- 翻译页：练习列表 + 翻译界面
- 写作页：三个子页（句型库、段落练习、全文模拟）
- 统计页：错题本 + 趋势图

## 开发约束
- 不使用任何云服务
- 所有数据仅本地存储
- UI适配手机（VIVO X200S），Flex布局
- TypeScript严格模式，避免any

## 当前已有数据文件
- miniprogram/data/sentences.ts（50条四级句子）
- 其余文件需创建空数组占位

## 本地代理服务器接口
- POST /correct_translation
- POST /correct_writing
- POST /teach_sentence
- GET /dictionary
- GET /dictionary/ai
- （需运行 server.js）

## 词典架构决策

### 翻译三层降级
1. **内置硬编码词典**（`server/dict_base.json`，3721词）— 永不依赖外网，最可靠的兜底
2. **免费翻译 API**（MyMemory）— 无需密钥，能连上就用
3. **AI 翻译**（DeepSeek）— 服务器有 Key 时的增强翻译
4. 任何翻译结果都保留英文原文，前端双语对照显示

### 持久化缓存
- 文件 `server/dict_cache.json`，启动时加载到内存 Map
- 查词成功后写缓存，每 30s 自动刷写到磁盘
- 加上 `dict_base.json` 做种子数据，常见词首次查询也有结果

### 批量翻译
- 大词汇量用独立脚本 `server/batch_translate.js` 离线分批翻译
- 优先用 MyMemory 免费 API（无需 Key），失败降级到 DeepSeek
- 用法：`cd server && node batch_translate.js`