.page {
  min-height: 100vh;
  padding: 32rpx;
  display: flex;
  flex-direction: column;
}

.page-header {
  margin-bottom: 32rpx;
}

.page-title {
  font-size: 44rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.page-subtitle {
  display: block;
  font-size: 26rpx;
  color: var(--blue);
  margin-top: 8rpx;
}

.trans-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.trans-card {
  background: var(--card-bg);
  border-radius: 28rpx;
  padding: 28rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 158, 181, 0.1);
}

.trans-card:active {
  transform: scale(0.98);
}

.trans-top {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 16rpx;
}

.trans-chinese {
  font-size: 30rpx;
  line-height: 1.6;
  color: var(--text-primary);
  flex: 1;
}

.trans-badge {
  font-size: 22rpx;
  color: var(--text-secondary);
  padding: 4rpx 16rpx;
  border-radius: 16rpx;
  background: var(--tag-bg);
  flex-shrink: 0;
}

.trans-badge.done {
  color: var(--mint);
  background: rgba(197, 225, 165, 0.2);
}

.trans-source {
  display: block;
  margin-top: 12rpx;
  font-size: 22rpx;
  color: var(--text-tertiary);
}

.detail-header {
  display: flex;
  align-items: center;
  gap: 20rpx;
  margin-bottom: 24rpx;
}

.back-btn {
  width: 100rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--tag-bg);
  border-radius: 16rpx;
  font-size: 26rpx;
  color: var(--text-secondary);
}

.detail-title {
  font-size: 34rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.detail-scroll {
  flex: 1;
}

.chinese-block {
  background: var(--card-bg);
  border-radius: 28rpx;
  padding: 32rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 158, 181, 0.1);
  margin-bottom: 24rpx;
}

.chinese-label {
  font-size: 24rpx;
  color: var(--text-secondary);
  display: block;
  margin-bottom: 12rpx;
}

.chinese-text {
  font-size: 32rpx;
  line-height: 1.7;
  color: var(--text-primary);
}

.answer-block {
  margin-bottom: 24rpx;
}

.answer-input {
  width: 100%;
  min-height: 200rpx;
  padding: 28rpx;
  border-radius: 24rpx;
  background: var(--card-bg);
  border: 2rpx solid var(--border);
  color: var(--text-primary);
  font-size: 28rpx;
  line-height: 1.7;
}

.submit-btn {
  width: 100%;
  height: 88rpx;
  border-radius: 44rpx;
  background: linear-gradient(135deg, #ff9eb5, var(--pink-dark));
  color: #fff;
  font-size: 30rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 32rpx;
  box-shadow: 0 8rpx 24rpx rgba(255, 158, 181, 0.3);
}

.result-block {
  background: var(--card-bg);
  border-radius: 28rpx;
  padding: 32rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 158, 181, 0.1);
  margin-bottom: 32rpx;
}

.result-score {
  display: flex;
  align-items: baseline;
  justify-content: center;
  gap: 8rpx;
  margin-bottom: 16rpx;
}

.score-num {
  font-size: 72rpx;
  font-weight: 700;
  color: #ff9eb5;
}

.score-label {
  font-size: 32rpx;
  color: var(--text-secondary);
}

.result-bar {
  height: 8rpx;
  border-radius: 4rpx;
  background: var(--tag-bg);
  margin-bottom: 24rpx;
  overflow: hidden;
}

.bar-fill {
  height: 100%;
  border-radius: 4rpx;
  background: linear-gradient(90deg, #ff9eb5, var(--pink-dark));
  transition: width 0.6s;
}

.result-suggestions {
  font-size: 28rpx;
  color: var(--text-primary);
  line-height: 1.7;
  margin-bottom: 20rpx;
}

.result-ref {
  padding: 20rpx;
  border-radius: 20rpx;
  background: var(--bg);
}

.ref-label {
  font-size: 24rpx;
  color: var(--text-secondary);
  display: block;
  margin-bottom: 8rpx;
}

.ref-text {
  font-size: 28rpx;
  color: var(--text-tertiary);
  line-height: 1.7;
}
