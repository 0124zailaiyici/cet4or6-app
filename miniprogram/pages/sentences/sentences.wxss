.page {
  min-height: 100vh;
  padding: 32rpx;
  display: flex;
  flex-direction: column;
}

.page-header {
  margin-bottom: 24rpx;
}

.title-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.dict-link {
  font-size: 36rpx;
  padding: 8rpx;
}

.page-title {
  font-size: 44rpx;
  font-weight: 700;
  color: var(--text-primary);
}

.page-subtitle {
  display: block;
  font-size: 26rpx;
  color: var(--mint);
  margin-top: 8rpx;
}

.search-bar {
  display: flex;
  align-items: center;
  background: var(--card-bg);
  border-radius: 24rpx;
  padding: 0 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 12rpx rgba(255, 158, 181, 0.08);
}

.search-input {
  flex: 1;
  height: 80rpx;
  font-size: 28rpx;
  color: var(--text-primary);
}

.search-clear {
  font-size: 28rpx;
  color: var(--text-secondary);
  padding: 8rpx;
}

.empty-tip {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 60rpx 0;
}

.empty-text {
  font-size: 28rpx;
  color: var(--text-secondary);
  margin-bottom: 8rpx;
}

.empty-hint {
  font-size: 24rpx;
  color: var(--text-secondary);
}

.topic-scroll {
  margin-bottom: 24rpx;
  white-space: nowrap;
}

.topic-bar {
  display: flex;
  gap: 16rpx;
}

.topic-tag {
  display: inline-block;
  padding: 12rpx 28rpx;
  border-radius: 24rpx;
  font-size: 26rpx;
  color: var(--text-secondary);
  background: var(--tag-bg);
}

.topic-tag.active {
  color: #ff9eb5;
  background: rgba(255, 158, 181, 0.15);
}

.card-scroll {
  flex: 1;
}

.card-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.sentence-card {
  background: var(--card-bg);
  border-radius: 28rpx;
  padding: 32rpx;
  box-shadow: 0 4rpx 20rpx rgba(255, 158, 181, 0.1);
  transition: all 0.3s;
}

.sentence-card.mastered {
  background: linear-gradient(135deg, #f5fff0, #ffffff);
  box-shadow: 0 4rpx 20rpx rgba(197, 225, 165, 0.2);
}

.card-english {
  font-size: 30rpx;
  line-height: 1.7;
  color: var(--text-primary);
}

.card-english .kw {
  color: #ff9eb5;
  font-style: normal;
  font-weight: 600;
}

.card-chinese {
  margin-top: 20rpx;
  padding: 20rpx;
  border-radius: 20rpx;
  background: var(--bg);
  font-size: 28rpx;
  color: var(--text-tertiary);
  line-height: 1.6;
}

.card-bottom {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 20rpx;
}

.card-left {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.card-topic {
  font-size: 22rpx;
  color: var(--text-secondary);
  padding: 4rpx 16rpx;
  border-radius: 16rpx;
  background: var(--tag-bg);
}

.fav-btn {
  font-size: 32rpx;
  color: var(--text-secondary);
  padding: 4rpx;
  line-height: 1;
}

.fav-btn.active {
  color: var(--gold);
}

.sentence-card.favorited {
  box-shadow: 0 4rpx 20rpx rgba(255, 215, 0, 0.12);
}

.master-btn {
  font-size: 24rpx;
  color: var(--text-secondary);
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
  background: var(--tag-bg);
}

.master-btn.active {
  color: var(--mint);
  background: rgba(197, 225, 165, 0.2);
}
